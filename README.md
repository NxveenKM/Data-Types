# Python Data Types

## Numeric Data Types
- **int**: Integer numbers (e.g., `5`, `-3`)
- **float**: Decimal numbers (e.g., `3.14`, `-2.5`)
- **complex**: Complex numbers (e.g., `3+4j`)

## Text Data Type
- **str**: Strings of text (e.g., `'hello'`, `"world"`)

## Sequence Data Types
- **list**: Ordered, mutable collection (e.g., `[1, 2, 3]`)
- **tuple**: Ordered, immutable collection (e.g., `(1, 2, 3)`)
- **range**: Sequence of numbers (e.g., `range(5)`)

## Mapping Data Type
- **dict**: Collection of key-value pairs (e.g., `{'name': 'Alice', 'age': 25}`)

## Set Data Types
- **set**: Unordered collection of unique items (e.g., `{1, 2, 3}`)
- **frozenset**: Immutable set (e.g., `frozenset([1, 2, 3])`)

## Boolean Data Type
- **bool**: True or False values (e.g., `True`, `False`)

---

This guide covers Python's main data types to help you get started with your programming. 
